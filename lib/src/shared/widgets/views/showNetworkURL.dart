import 'package:flutter/material.dart'
    show BuildContext, StatelessWidget, Text, Widget;
import 'package:flutter/src/foundation/key.dart';

/*
class ShowNetworkURL extends StatelessWidget {
  final Record record;
  final Network<Map> _networkURL;

  // https://stackoverflow.com/questions/52449508/constructor-optional-params
  // const ShowNetworkURL({Key key, this.record, this._networkURL}) : super(key: key);
  const ShowNetworkURL([Key key, this.record, this._networkURL]) : super(key: key);

  @override
  Widget build(BuildContext context) {
    // return _networkURL.URL;
    // TODO: need to test this "fix" 11/29/2021
    return new Text(_networkURL.URL);
  }
}

 */